import { CadenaValidationDirective } from './cadena-validation.directive';

describe('CadenaValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CadenaValidationDirective();
    expect(directive).toBeTruthy();
  });
});
