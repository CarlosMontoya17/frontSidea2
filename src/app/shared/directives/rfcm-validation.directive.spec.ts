import { RfcmValidationDirective } from './rfcm-validation.directive';

describe('RfcmValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new RfcmValidationDirective();
    expect(directive).toBeTruthy();
  });
});
