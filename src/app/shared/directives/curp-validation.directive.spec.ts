import { CurpValidationDirective } from './curp-validation.directive';

describe('CurpValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CurpValidationDirective();
    expect(directive).toBeTruthy();
  });
});
