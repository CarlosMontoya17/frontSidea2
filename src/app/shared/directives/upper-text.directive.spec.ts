import { UpperTextDirective } from './upper-text.directive';

describe('UpperTextDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperTextDirective();
    expect(directive).toBeTruthy();
  });
});
