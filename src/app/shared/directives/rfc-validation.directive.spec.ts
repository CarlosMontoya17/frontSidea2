import { RfcValidationDirective } from './rfc-validation.directive';

describe('RfcValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new RfcValidationDirective();
    expect(directive).toBeTruthy();
  });
});
