import { NoDownloadPipe } from './no-download.pipe';

describe('NoDownloadPipe', () => {
  it('create an instance', () => {
    const pipe = new NoDownloadPipe();
    expect(pipe).toBeTruthy();
  });
});
